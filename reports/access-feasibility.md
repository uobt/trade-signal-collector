# Доступность источников — проверка 2026-09-17/18

Все проверки выполнены из текущей среды пользователя (web_fetch, без
обхода защит). Обход CAPTCHA/WAF, прокси-ротация и копирование cookies
запрещены ТЗ и не применялись.

## ImportYeti (контур B, уровень компаний)

| Проверка | Результат |
|---|---|
| robots.txt | HTTP 200. `User-agent: *` → `Allow: /`; `Disallow: /search?q`, `/api/addresses`, `/api/ep`, `/cdn-cgi/`, `/mx/`, `/us-export/`, `/partnerships/` |
| Главная | HTTP 403, тело — Cloudflare «Just a moment…» challenge |
| Профиль компании (`/company/walmart`) | HTTP 403, тот же challenge |

Вывод: формально профильные страницы robots не запрещены, но фактически
доступ закрыт WAF для нашей среды. Статус **BLOCKED (technical)**.
Повторные попытки не выполнялись. Единственный будущий путь —
санкционированная владельцем выгрузка/партнёрство либо платный провайдер
таможенных данных (Panjiva/Volza/ImportGenius — по отдельному бюджету).

## UN Comtrade (контур A, статистика)

| Проверка | Результат |
|---|---|
| `GET /data/v1/get/C/A/HS?...` без ключа | HTTP 401 `Access denied due to missing subscription key` |
| `GET /public/v1/preview/C/A/HS?...` без ключа | **HTTP 200, полные данные** (проверено 2026-09-18) |
| preview, второй запрос подряд | HTTP 429; после паузы 75с → 200 (лимит ~1 запрос/мин) |
| preview, `period=2023,2024` | HTTP 400 `Maximum number of periods for preview is 1` |
| структура preview | иерархия с total-строками (partner2=0, customs=C00, mot=0) и их разбивками; C00 = C01 + C04; наивная сумма удваивает |

Вывод: preview — живой бесплатный канал (ограничения: 1 период/запрос,
~1 запрос/мин). Парсер берёт total-строки; контрольная сумма разбивок
DEU×CHN×HS8422×2024 совпала с total до центов ($141,462,937.43).
Live-прогон 2026-09-18: 6/6 запросов success (DEU/GBR/USA × 2023/2024).
Статус **comtrade-preview: LIVE_VERIFIED**; полный канал `comtrade - v1`
(data/v1/get, мульти-период, aggregateBy) — по ключу после регистрации.

## Итоговая матрица

| Источник | Уровень | Статус | Что дальше |
|---|---|---|---|
| comtrade-preview | страны/HS | **LIVE_VERIFIED** | работает; 1 период/запрос, ~1 запрос/мин |
| comtrade data/v1 | страны/HS | KEY_REQUIRED | регистрация ключа владельцем → полный канал |
| importyeti | компании | BLOCKED | только санкционированная выгрузка |
| census | страны/HS | не проверен | пилот после ключа |
| uktradeinfo | страны/HS | не проверен | пилот после ключа |
| csvimport | оба | LIVE_VERIFIED | работает (demo-прогон 2026-09-18) |
